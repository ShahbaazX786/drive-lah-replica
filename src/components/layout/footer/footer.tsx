import "./footer.scss";

const Footer = () => {
  return (
    <section id="bottom-navigation-wrapper">
      <section id="bottom-navigation">
        <button>Next</button>
      </section>
    </section>
  );
};

export default Footer;
